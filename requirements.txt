# Requirements pour Python 3.11.9

# Core dependencies
numpy==1.26.4
Pillow==10.3.0
opencv-python-headless==4.8.1.78

# Deep Learning (CPU-only versions for Heroku - reduces slug size from 2.5GB to ~500MB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.0.1
torchvision==0.15.2

# Web frameworks
Flask==3.0.0
gunicorn==21.2.0
streamlit==1.28.0
requests==2.31.0

# Image processing (minimal - albumentations removed to save space)
# albumentations==1.3.1

# Modèles de segmentation avancés
segmentation-models-pytorch==0.3.3
# timm sera installé automatiquement par segmentation-models-pytorch (version 0.9.2)